export type User = {
  // name: string;
  email: string;
  password: string;
  uid: string;
};

export type Product = {
  name: string;
  price: number;
  description: string;
  category: string;
  stock: number;
  id: string;
};